---
import type { APIRoute } from 'astro';

export const GET: APIRoute = async ({ params, redirect }) => {
  const { mangaId, chapterId } = params;
  if (!mangaId || !chapterId) {
    return redirect('/404');
  }
  return redirect(`/manga/${mangaId}/chapter/${chapterId}`);
};
--- 